#include <stdio.h>
#include "main.h"

/**
 * mul - Multiplies teo integers
 * @a: the first integer
 * @b: the second integer to be computed
 *
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
