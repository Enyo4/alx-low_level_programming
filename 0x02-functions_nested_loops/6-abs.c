#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @n: The number to be computed
 *
 * Return: 0 on success
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
