#include "main.h"

/**
 * add - A function that adds two integers and returns the result
 * @fNum: first number to be computed
 * @sNum: second number to be computed
 *
 * Return: 0;
 */
int add(int fNum, int sNum)
{
	return (fNum + sNum);
}
